// As disciplinas serão adicionadas via API do MLAB, porém para exercitar alguns conceitos
// vou adicionar a verificação do role. Teoricamente só um admin poderia fazer o CRUD das disciplinas.
const Disciplina = require("../../models/Disciplina");

module.exports = {
  cadastraOuAtualizaDisciplina(req, res) {},
  verTodas(req, res) {},
  verDisciplina(req, res) {},
  apagaDisciplina(req, res) {}
};
